/* Default styles - generic(pre css), settings (global settings),
tools (mixins, functions), utilities(high specify CSS) and elements (unclassed selectors)
========================================================================== */
import generic from 'sass/generic';
import elements from 'sass/elements';
import tools from 'sass/tools';
import settings from 'sass/settings';
import utilities from 'sass/utilities';
